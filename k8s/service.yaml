# k8s/service.yaml
apiVersion: v1
kind: Service
metadata:
  name: springboot-mvc-demo
spec:
  type: NodePort
  selector:
    app: springboot-mvc-demo
  ports:
    - protocol: TCP
      port: 9090
      targetPort: 9090
